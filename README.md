# Gramedia Product Page (demo)

This is a small React + Vite demo that reproduces the Gramedia product page layout from the provided screenshot.

Run:

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
